class Solution {
public:
    // bool find(vector<int>& piles,int turn,int i,int n){
    //     if(i==n){
    //         return 0;
    //     }

    // }
    bool stoneGame(vector<int>& piles) {
        //int n=piles.size();
        //return find(piles,0,0,n);
        //kahi se start kare chahe first se ya last se jitega to alice hi
        return true;
    }
};
